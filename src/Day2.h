#pragma once

// Function PROTOTYPES
void Day2();

// Every function has its own type, name and arguments which can be initialized, 
// but if we decide to do so, we have to put that arguments as the last one
// We should declare our function structure in .cpp file where we include this header file
void ExampleFunction(int x, int y, int z = 0);

void Exercise3();
void Exercise4();
void Exercise5();

int Addition(int x, int y);
int Subtraction(int x, int y);
